# SUMMARY:
# update, upgrade, git, vim, ncdu, fzf, tmux, cleanup

sudo apt update
sudo apt upgrade -y
sudo apt install -y vim git ncdu fzf, tmux
# create/append all files listed below
# cleanup
sudo apt clean
sudo rm /etc/ssh/ssh_host*



#### create or add to these files (boot is in separate partition) ####

# boot/ssh

# boot/wpa_supplicant.conf
country=ca
update_config=1
ctrl_interface=/var/run/wpa_supplicant
network={
 ssid="The Fast One"
 psk="IMakeStuff"
}
network={
 ssid="SKTechworksAP"
 psk="IMakeStuff"
}

# ~/.bashrc
if [[ -z "$TMUX" ]]; then
  tmux a -t init
fi
alias history="fc -ln 1 | fzf"

# ~/.vimrc
inoremap jk <Esc>

# /etc/systemd/system/tmux.service
[Unit]
Description=Tmux on startup
StartLimitIntervalSec=0
[Service]
Type=simple
Restart=always
RestartSec=10
User=pi
ExecStart=/bin/bash /etc/systemd/system/tmux.bash
[Install]
WantedBy=multi-user.target

# /etc/systemd/system/tmux.bash
#!/bin/bash
tmux start-server
tmux new -d -s vim
tmux send-keys -t vim.0 "vim" ENTER
tmux new -d -s init
while :; do sleep 2073600; done

# /etc/rc.local
ssh-keygen -A 
