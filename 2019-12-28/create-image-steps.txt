# SUMMARY:
# a lightweight (less than 2GB) set of general use tools
# update, upgrade, git, vim, ncdu, fzf, tmux, caca-utils, cleanup

sudo apt update
sudo apt upgrade -y
sudo apt install -y vim git ncdu fzf tmux caca-utils
git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm
# create/append all files listed below
# inside tmux, run tmux-plugins plugin installer (^b^I)
# cleanup
sudo apt clean
sudo rm /etc/ssh/ssh_host*


#### create or add to these files (boot is in separate partition) ####

# boot/ssh

# boot/wpa_supplicant.conf
country=ca
update_config=1
ctrl_interface=/var/run/wpa_supplicant
network={
 ssid="The Fast One"
 psk="IMakeStuff"
}
network={
 ssid="SKTechworksAP"
 psk="IMakeStuff"
}

# ~/.bashrc
if [[ -z "$TMUX" ]]; then
  tmux a || tmux
fi
alias history="fc -ln 1 | fzf"
alias tk="tmux list-keys | fzf"
alias tc="tmux list-commands | fzf"

# ~/.vimrc
inoremap jk <Esc>

# ~/.tmux.conf
# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @continuum-boot 'on'
set -g @continuum-restore 'on'
# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run -b '~/.tmux/plugins/tpm/tpm'

# /etc/rc.local
ssh-keygen -A
